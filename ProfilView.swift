
import SwiftUI

struct ProfilView: View {
    var body: some View {
        VStack {
            Text("Profil utilisateur")
            Text("À compléter dans les prochaines versions.")
        }
        .padding()
    }
}
